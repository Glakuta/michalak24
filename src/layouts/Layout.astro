---
import "../assets/styles/global.css"
import Footer_section from "../components/shared/footer_component/footer_section.astro";
import Navigation_component from "../components/shared/navigation_component/navigation_component.astro";
import Socialmedia_sidebar_component
    from "../components/shared/socialmedia_sidebar_component/socialmedia_sidebar_component.astro";
const {pathname} = Astro.url
const isHome = pathname === "/" || pathname === "/index.html";
---

<!doctype html>
<html lang="pl" class="scroll-smooth">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<link rel="icon" href="/favicon.ico" />
		<meta name="generator" content={Astro.generator} />
		<title>Pomoc drogowa Kalisz - Michalak24</title>
	</head>
	<body>
    <Navigation_component />
    <Socialmedia_sidebar_component />
		<slot />
    <Footer_section />
	</body>
</html>

<style>
	html,
	body {
		margin: 0;
		width: 100%;
		height: 100%;
        background-color: #18181b;
	}
</style>

{isHome && (
<script>
    const navbar = document.getElementById("navbar");
    const hero = document.getElementById("hero");

    if (hero && navbar) {
        const observer = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (!entry.isIntersecting) {
                    navbar.classList.replace("bg-transparent", "bg-yellow-400");
                } else {
                    navbar.classList.replace("bg-yellow-400", "bg-transparent");
                }
            });
        }, { threshold: 0.1 });

        observer.observe(hero);
    }
</script>
    )}
